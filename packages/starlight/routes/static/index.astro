---
import { paths } from '../../utils/routing/index.js';
import CommonPage from '../common.astro';

export const prerender = true;

export async function getStaticPaths() {
	return paths;
}
---

<CommonPage />
